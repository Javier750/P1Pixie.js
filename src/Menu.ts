/*
import { Container, Sprite } from "pixi.js";

export class GollumHat extends Container{

constructor(){
    super();

        const gollum: Sprite = Sprite.from("Gollum");

	    const hat: Sprite = Sprite.from("Hat");

	    hat.position.set(5, 5);

	    this.addChild(gollum);
	    this.addChild(hat);
    }
};
*/